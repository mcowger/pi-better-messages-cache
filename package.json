{
  "name": "@mcowger/pi-better-messages-cache",
  "version": "1.4.0",
  "description": "Pi extension: dual cache-breakpoint strategy for Anthropic models — marks both the last assistant tool_use block and the last user message block with cache_control, dramatically improving cache hit rates on MiniMax, Kimi, and other Anthropic-compatible providers.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "anthropic",
    "cache",
    "prompt-caching",
    "pi-coding-agent"
  ],
  "license": "MIT",
  "author": "mcowger",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mcowger/pi-better-messages-cache.git"
  },
  "type": "module",
  "files": [
    "index.ts",
    "test"
  ],
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "echo 'nothing to check'",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },

  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@sinclair/typebox": "*"
  }
}
